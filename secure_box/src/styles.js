import { StyleSheet } from "react-native";

export const colors = {
  primary: '#007bff',
  secondary: '#6c757d',
  background: '#f8f9fa',
};

export default StyleSheet.create({
  fill: {
    flex: 1
  }
});
